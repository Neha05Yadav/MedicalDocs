"use client";

import Link from "next/link";
import { useRouter } from "next/navigation";
import { useEffect, useMemo, useState } from "react";
import { toast } from "sonner";
import { Activity, AlertTriangle, ArrowRight, BarChart3, Bell, Boxes, CheckCircle2, Clock3, CreditCard, Download, FileText, IndianRupee, MapPin, PackageCheck, Pill, Plus, Search, Send, ShoppingBag, Truck, Upload, UserRound } from "lucide-react";
import { deliveries, inventory, medicines, notifications, orders, patients, quotations, requests } from "./pharmacy-data";

export type PharmacyView = "overview" | "requests" | "quotations" | "orders" | "inventory" | "alternatives" | "deliveries" | "billing" | "patients" | "notifications" | "analytics" | "profile";

const badgeTone = (status: string) => {
  const value = status.toLowerCase();
  if (["accepted", "paid", "delivered", "completed", "in stock", "available"].some((item) => value.includes(item))) return "bg-emerald-50 text-emerald-700 ring-emerald-200";
  if (["new", "viewed", "preparing", "out for delivery", "quotation sent"].some((item) => value.includes(item))) return "bg-cyan-50 text-cyan-700 ring-cyan-200";
  if (["pending", "partial", "low stock", "near expiry", "ready"].some((item) => value.includes(item))) return "bg-amber-50 text-amber-700 ring-amber-200";
  return "bg-rose-50 text-rose-700 ring-rose-200";
};

const Badge = ({ children }: { children: React.ReactNode }) => <span className={`inline-flex whitespace-nowrap rounded-full px-2.5 py-1 text-[11px] font-extrabold ring-1 ring-inset ${badgeTone(String(children))}`}>{children}</span>;

const Page = ({ title, eyebrow, description, actions, children }: { title: string; eyebrow: string; description: string; actions?: React.ReactNode; children: React.ReactNode }) => (
  <div className="min-h-screen space-y-6 p-4 sm:p-6 lg:p-8">
    <header className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-xs font-black uppercase tracking-[.2em] text-emerald-600">{eyebrow}</p><h1 className="mt-1 text-3xl font-black text-slate-950">{title}</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-500">{description}</p></div>{actions && <div className="flex flex-wrap gap-2">{actions}</div>}</header>
    {children}
  </div>
);

const SearchBar = ({ value, onChange, placeholder }: { value: string; onChange: (value: string) => void; placeholder: string }) => <label className="relative block w-full sm:w-80"><Search className="absolute left-3.5 top-1/2 size-4 -translate-y-1/2 text-slate-400"/><input value={value} onChange={(event) => onChange(event.target.value)} placeholder={placeholder} className="h-11 w-full rounded-xl border border-slate-200 bg-white pl-10 pr-4 text-sm outline-none transition focus:border-emerald-500 focus:ring-4 focus:ring-emerald-500/10"/></label>;

const Card = ({ children, className = "" }: { children: React.ReactNode; className?: string }) => <section className={`rounded-2xl border border-slate-200 bg-white shadow-sm ${className}`}>{children}</section>;

const EmptySafeTable = ({ columns, rows }: { columns: string[]; rows: React.ReactNode[][] }) => <div className="overflow-x-auto"><table className="w-full min-w-[850px] text-left text-sm"><thead><tr className="border-b bg-slate-50/80 text-[11px] font-black uppercase tracking-wider text-slate-500">{columns.map((column, columnIndex) => <th key={`${column}-${columnIndex}`} className="px-5 py-4">{column}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{rows.map((row, index) => <tr key={index} className="transition hover:bg-emerald-50/25">{row.map((cell, cellIndex) => <td key={cellIndex} className="px-5 py-4 text-slate-600">{cell}</td>)}</tr>)}</tbody></table></div>;

const ActionLink = ({ href, label = "Open" }: { href: string; label?: string }) => <Link href={href} className="inline-flex items-center gap-1.5 rounded-lg border border-emerald-200 bg-emerald-50 px-3 py-2 text-xs font-bold text-emerald-700 transition hover:bg-emerald-100">{label}<ArrowRight className="size-3.5"/></Link>;

export default function PharmacyUI({ view }: { view: PharmacyView }) {
  const [query, setQuery] = useState("");
  const [filter, setFilter] = useState("All");
  if (view === "overview") return <Overview/>;
  if (view === "requests") return <Requests query={query} setQuery={setQuery} filter={filter} setFilter={setFilter}/>;
  if (view === "quotations") return <Quotations query={query} setQuery={setQuery}/>;
  if (view === "orders") return <Orders query={query} setQuery={setQuery} filter={filter} setFilter={setFilter}/>;
  if (view === "inventory") return <Inventory query={query} setQuery={setQuery}/>;
  if (view === "alternatives") return <Alternatives/>;
  if (view === "deliveries") return <Deliveries/>;
  if (view === "billing") return <Billing/>;
  if (view === "patients") return <Patients query={query} setQuery={setQuery}/>;
  if (view === "notifications") return <Notifications/>;
  if (view === "analytics") return <Analytics/>;
  return <Profile/>;
}

function Overview() {
  const metrics = [
    ["New Requests", "12", FileText, "text-blue-600 bg-blue-50"], ["Pending Quotations", "7", Clock3, "text-amber-600 bg-amber-50"], ["Accepted Orders", "18", PackageCheck, "text-emerald-600 bg-emerald-50"], ["Preparing Orders", "6", Boxes, "text-violet-600 bg-violet-50"],
    ["Out for Delivery", "4", Truck, "text-cyan-600 bg-cyan-50"], ["Completed Orders", "31", CheckCircle2, "text-green-600 bg-green-50"], ["Today's Revenue", "₹28,640", IndianRupee, "text-teal-600 bg-teal-50"], ["Low Stock Medicines", "9", AlertTriangle, "text-rose-600 bg-rose-50"],
  ] as const;
  return <Page title="Pharmacy operations" eyebrow="MediDoc Pharmacy" description="Receive patient-shared prescriptions, quote medicines and manage fulfilment without accessing complete medical history.">
    <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{metrics.map(([label, value, Icon, tone]) => <Card key={label} className="p-5"><div className="flex items-start justify-between"><div><p className="text-xs font-extrabold uppercase tracking-wider text-slate-500">{label}</p><p className="mt-3 text-2xl font-black text-slate-950">{value}</p></div><span className={`rounded-xl p-3 ${tone}`}><Icon className="size-5"/></span></div></Card>)}</div>
    <div className="grid gap-6 xl:grid-cols-[1.35fr_.65fr]"><Card><div className="flex items-center justify-between border-b p-5"><div><h2 className="font-black text-slate-900">Recent prescription requests</h2><p className="mt-1 text-xs text-slate-500">Prescriptions explicitly shared for medicine ordering</p></div><ActionLink href="/pharmacy/prescription-requests" label="View all"/></div><EmptySafeTable columns={["Request","Patient","Doctor","Location","Delivery","Status","Action"]} rows={requests.slice(0,3).map((row) => [<b key="id" className="text-slate-900">{row.id}</b>, row.patient, <span key="doc">{row.doctor}<small className="block text-slate-400">{row.facility}</small></span>, <span key="loc">{row.location}<small className="block text-slate-400">{row.distance}</small></span>, row.delivery, <Badge key="s">{row.status}</Badge>, <ActionLink key="a" href={`/pharmacy/prescription-requests/${row.id}`}/>])}/></Card><MiniCharts/></div>
    <Card><div className="flex items-center justify-between border-b p-5"><h2 className="font-black text-slate-900">Recent orders</h2><ActionLink href="/pharmacy/orders" label="Manage orders"/></div><EmptySafeTable columns={["Order","Patient","Amount","Payment","Delivery","Status","Date","Action"]} rows={orders.map((row) => [<b key="id" className="text-slate-900">{row.id}</b>,row.patient,row.amount,<Badge key="p">{row.payment}</Badge>,row.delivery,<Badge key="s">{row.status}</Badge>,row.date,<ActionLink key="a" href="/pharmacy/orders"/>])}/></Card>
  </Page>;
}

function MiniCharts() { const bars=[52,68,43,76,89,64,94]; return <Card className="p-5"><div className="flex items-center justify-between"><div><h2 className="font-black text-slate-900">Weekly orders</h2><p className="mt-1 text-xs text-slate-500">Acceptance rate 78%</p></div><BarChart3 className="size-5 text-emerald-600"/></div><div className="mt-8 flex h-40 items-end gap-3">{bars.map((height,index)=><div key={index} className="flex flex-1 flex-col items-center gap-2"><div className="w-full rounded-t-lg bg-gradient-to-t from-emerald-600 to-cyan-400" style={{height:`${height}%`}}/><span className="text-[10px] font-bold text-slate-400">{["M","T","W","T","F","S","S"][index]}</span></div>)}</div><div className="mt-5 rounded-xl bg-emerald-50 p-4"><p className="text-xs font-bold text-emerald-700">Top medicine</p><p className="mt-1 font-black text-slate-900">Telmisartan 40 mg</p><p className="text-xs text-slate-500">146 strips sold this month</p></div></Card>; }

function Requests({query,setQuery,filter,setFilter}:any) { const [liveRequests,setLiveRequests]=useState<any[]>([]); useEffect(()=>{ const token=localStorage.getItem("token"); fetch("/api/pharmacy/prescription-requests",{headers:{Authorization:`Bearer ${token}`}}).then(async response=>{const data=await response.json().catch(()=>[]);if(!response.ok)throw new Error(data?.message||"Could not load pharmacy requests.");return data;}).then(data=>setLiveRequests((Array.isArray(data)?data:[]).map((row:any)=>({id:row.id,patient:row.patient,prescription:row.prescription,doctor:"Patient request",facility:`Patient ID: ${row.patientId}`,location:row.location,date:new Date(row.createdAt).toLocaleDateString("en-IN",{day:"2-digit",month:"short",year:"numeric"}),delivery:"Home delivery",status:String(row.status||"NEW").toLowerCase()==="new"?"New":row.status})))).catch(error=>toast.error(error.message)); },[]); const statuses=["All","New","Viewed","Quotation Sent","Accepted","Rejected","Expired"]; const allRequests=[...liveRequests,...requests].filter((row,index,array)=>array.findIndex(item=>item.id===row.id)===index); const visible=allRequests.filter(row=>(filter==="All"||row.status===filter)&&JSON.stringify(row).toLowerCase().includes(query.toLowerCase())); return <Page title="Prescription Requests" eyebrow="Patient-shared access" description="Only prescriptions shared by patients for medicine fulfilment are visible. Complete medical history remains private." actions={<SearchBar value={query} onChange={setQuery} placeholder="Search requests..."/>}><div className="flex flex-wrap gap-2">{statuses.map(status=><button key={status} onClick={()=>setFilter(status)} className={`rounded-full px-4 py-2 text-xs font-bold ${filter===status?"bg-emerald-600 text-white":"border bg-white text-slate-600"}`}>{status}</button>)}</div><Card><EmptySafeTable columns={["Request ID","Patient","Prescription","Doctor / Facility","Location","Requested","Delivery","Status","Action"]} rows={visible.map(row=>[<b key="id">{row.id}</b>,row.patient,row.prescription,<span key="d">{row.doctor}<small className="block text-slate-400">{row.facility}</small></span>,row.location,row.date,row.delivery,<Badge key="s">{row.status}</Badge>,<ActionLink key="a" href={`/pharmacy/prescription-requests/${row.id}`}/>])}/></Card></Page>; }

function Quotations({query,setQuery}:any) { const [savedQuotations,setSavedQuotations]=useState<any[]>([]); useEffect(()=>{ try { const actions=JSON.parse(localStorage.getItem("pharmacyQuotationActions")||"[]"); setSavedQuotations(actions.filter((item:any)=>item.status!=="Rejected").map((item:any,index:number)=>({id:item.id||`QUO-SAVED-${index+1}`,patient:item.patient||requests[0]?.patient||"Patient",prescription:item.prescription||requests[0]?.prescription||"Prescription",amount:item.formattedAmount||`₹${Number(item.amount||0).toLocaleString("en-IN")}`,sent:item.sent||new Date(item.updatedAt||Date.now()).toLocaleDateString("en-IN",{day:"2-digit",month:"short",year:"numeric"}),valid:item.valid||"6 hours",response:item.response||"Awaiting response",status:item.status==="Quotation Sent"?"Pending":item.status||"Draft"}))); } catch { setSavedQuotations([]); } },[]); const allRows=[...savedQuotations,...quotations].filter((row,index,array)=>array.findIndex(item=>item.id===row.id)===index); const visible=allRows.filter(row=>JSON.stringify(row).toLowerCase().includes(query.toLowerCase())); return <Page title="Quotations" eyebrow="Pricing workspace" description="Prepare transparent medicine pricing and track patient responses." actions={<><SearchBar value={query} onChange={setQuery} placeholder="Search quotations..."/><ActionLink href="/pharmacy/quotations/create" label="Create quotation"/></>}><Card><EmptySafeTable columns={["Quotation","Patient","Prescription","Amount","Sent","Valid until","Patient response","Status","Action"]} rows={visible.map(row=>[<b key="id">{row.id}</b>,row.patient,row.prescription,<b key="amount">{row.amount}</b>,row.sent,row.valid,row.response,<Badge key="s">{row.status}</Badge>,<ActionLink key="a" href="/pharmacy/quotations/create"/>])}/></Card></Page>; }

function Orders({query,setQuery,filter,setFilter}:any) { const stages=["All","Confirmed","Payment Pending","Paid","Preparing","Ready for Pickup","Out for Delivery","Delivered","Cancelled"]; const visible=orders.filter(row=>(filter==="All"||row.status===filter||row.payment===filter)&&JSON.stringify(row).toLowerCase().includes(query.toLowerCase())); return <Page title="Orders" eyebrow="Medicine fulfilment" description="Manage confirmed orders through payment, preparation, pickup or delivery." actions={<SearchBar value={query} onChange={setQuery} placeholder="Search orders..."/>}><div className="flex flex-wrap gap-2">{stages.map(stage=><button key={stage} onClick={()=>setFilter(stage)} className={`rounded-full px-3 py-2 text-xs font-bold ${filter===stage?"bg-emerald-600 text-white":"border bg-white text-slate-600"}`}>{stage}</button>)}</div><Card><EmptySafeTable columns={["Order","Patient","Prescription","Amount","Payment","Delivery","Status","Date","Action"]} rows={visible.map(row=>[<b key="id">{row.id}</b>,row.patient,row.prescription,row.amount,<Badge key="p">{row.payment}</Badge>,row.delivery,<Badge key="s">{row.status}</Badge>,row.date,<ActionLink key="a" href="/pharmacy/orders"/>])}/></Card></Page>; }

function Inventory({query,setQuery}:any) { const visible=inventory.filter(row=>JSON.stringify(row).toLowerCase().includes(query.toLowerCase())); return <Page title="Inventory" eyebrow="Stock control" description="Track batches, stock thresholds, expiry and medicine pricing." actions={<><SearchBar value={query} onChange={setQuery} placeholder="Search inventory..."/><button className="rounded-xl bg-emerald-600 px-4 py-2.5 text-sm font-bold text-white"><Plus className="mr-2 inline size-4"/>Add Medicine</button></>}><div className="flex flex-wrap gap-2"><button className="rounded-xl border bg-white px-4 py-2 text-xs font-bold"><Upload className="mr-2 inline size-4"/>Import Inventory</button><button className="rounded-xl border bg-white px-4 py-2 text-xs font-bold"><Download className="mr-2 inline size-4"/>Export Inventory</button></div><Card><EmptySafeTable columns={["Medicine","Brand / Composition","Strength","Category","Batch","Stock","Expiry","MRP / Selling","Status","Action"]} rows={visible.map(row=>[<b key="m">{row.medicine}</b>,<span key="b">{row.brand}<small className="block text-slate-400">{row.composition}</small></span>,row.strength,row.category,row.batch,<span key="st"><b>{row.stock}</b><small className="block text-slate-400">Min {row.minimum}</small></span>,row.expiry,<span key="pr">{row.mrp}<small className="block text-emerald-600">{row.price}</small></span>,<Badge key="s">{row.status}</Badge>,<button key="a" className="text-xs font-bold text-emerald-700">Update stock</button>])}/></Card></Page>; }

function Alternatives() { return <Page title="Alternative Medicines" eyebrow="Patient approval required" description="A suggested substitute is never added to an order until the patient approves it."><div className="grid gap-4 lg:grid-cols-2">{medicines.filter(item=>item.availability!=="Available").map(item=><Card key={item.name} className="p-5"><div className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase text-slate-400">Prescribed medicine</p><h3 className="mt-1 font-black text-slate-900">{item.name}</h3></div><Badge>{item.availability}</Badge></div><div className="my-5 flex items-center gap-3"><div className="h-px flex-1 bg-slate-200"/><RefreshIcon/><div className="h-px flex-1 bg-slate-200"/></div><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs font-bold text-slate-400">Suggested equivalent</p><p className="mt-1 font-bold text-slate-800">Equivalent composition — same strength</p></div><button className="mt-4 w-full rounded-xl bg-emerald-600 py-3 text-sm font-bold text-white">Send for patient approval</button></Card>)}</div></Page>; }
const RefreshIcon=()=> <Activity className="size-4 text-emerald-600"/>;

function Deliveries() { return <Page title="Delivery Management" eyebrow="Last-mile fulfilment" description="Coordinate store pickup and home delivery without exposing clinical information."><Card><EmptySafeTable columns={["Delivery","Order","Patient","Address / Contact","Partner","ETA","Charge","Status","Action"]} rows={deliveries.map(row=>[<b key="id">{row.id}</b>,row.order,row.patient,<span key="a">{row.address}<small className="block text-slate-400">{row.contact}</small></span>,row.partner,row.eta,row.charge,<Badge key="s">{row.status}</Badge>,<button key="x" className="text-xs font-bold text-emerald-700">Track</button>])}/></Card></Page>; }

function Billing() { return <Page title="Payments & Billing" eyebrow="Transaction ledger" description="Review order invoices and payment settlement status."><Card><EmptySafeTable columns={["Invoice","Order","Patient","Amount","Mode","Transaction","Status","Payment date","Invoice"]} rows={orders.map((row,index)=>[`INV-PH-${2608040+index}`,row.id,row.patient,row.amount,row.payment,index===2?"COD":"TXN8A26"+(71+index),<Badge key="s">{row.payment}</Badge>,row.date,<button key="d" className="rounded-lg border p-2 text-emerald-700"><Download className="size-4"/></button>])}/></Card></Page>; }

function Patients({query,setQuery}:any) { const visible=patients.filter(row=>JSON.stringify(row).toLowerCase().includes(query.toLowerCase())); return <Page title="Patients" eyebrow="Order contacts only" description="Only patient identity, contact and prescription-order history shared with this pharmacy are visible." actions={<SearchBar value={query} onChange={setQuery} placeholder="Search patients..."/>}><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{visible.map(row=><Card key={row.id} className="p-5"><div className="flex items-center gap-3"><span className="grid size-11 place-items-center rounded-xl bg-emerald-50 text-emerald-700"><UserRound className="size-5"/></span><div><h3 className="font-black text-slate-900">{row.name}</h3><p className="text-xs text-slate-500">{row.id}</p></div></div><div className="mt-5 space-y-2 text-sm text-slate-600"><p>{row.contact}</p><p><MapPin className="mr-2 inline size-4"/>{row.location}</p><p>{row.requests} prescription requests · Last order {row.lastOrder}</p></div></Card>)}</div></Page>; }

function Notifications() { return <Page title="Notifications" eyebrow="Pharmacy alerts" description="Prescription, quotation, order, stock and delivery updates."><div className="mx-auto max-w-4xl space-y-3">{notifications.map(item=><Card key={item.title} className="flex items-start gap-4 p-5"><span className="rounded-xl bg-emerald-50 p-3 text-emerald-700"><Bell className="size-5"/></span><div className="flex-1"><div className="flex justify-between gap-4"><h3 className="font-bold text-slate-900">{item.title}</h3><span className="text-xs text-slate-400">{item.time}</span></div><p className="mt-1 text-sm text-slate-600">{item.message}</p></div></Card>)}</div></Page>; }

function Analytics() { const months=[48,63,58,77,72,91]; return <Page title="Reports & Analytics" eyebrow="Business intelligence" description="Sales, order, quotation, stock, expiry and delivery performance."><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[["Total sales","₹4,82,650"],["Total orders","428"],["Average order value","₹1,128"],["Quotation acceptance","78%"]].map(([label,value])=><Card key={label} className="p-5"><p className="text-xs font-bold uppercase text-slate-400">{label}</p><p className="mt-3 text-2xl font-black text-slate-950">{value}</p></Card>)}</div><div className="grid gap-6 lg:grid-cols-2"><Card className="p-6"><h2 className="font-black">Monthly revenue</h2><div className="mt-8 flex h-64 items-end gap-5">{months.map((height,index)=><div key={index} className="flex flex-1 flex-col items-center gap-2"><div className="w-full rounded-t-xl bg-gradient-to-t from-emerald-600 to-cyan-400" style={{height:`${height}%`}}/><span className="text-xs text-slate-400">{["Mar","Apr","May","Jun","Jul","Aug"][index]}</span></div>)}</div></Card><Card className="p-6"><h2 className="font-black">Operational reports</h2><div className="mt-5 space-y-3">{[["Low stock report","9 medicines"],["Near-expiry report","4 batches"],["Delivery performance","94% on time"],["Most sold medicine","Telmisartan 40 mg"]].map(([label,value])=><div key={label} className="flex justify-between rounded-xl bg-slate-50 p-4"><span className="font-semibold text-slate-600">{label}</span><b>{value}</b></div>)}</div></Card></div></Page>; }

function Profile() { const [pharmacyId,setPharmacyId]=useState("PHM00001"); useEffect(()=>{ try { const user=JSON.parse(localStorage.getItem("user")||"{}"); if(user.hospitalId) setPharmacyId(String(user.hospitalId)); } catch {} },[]); const fields=[["Pharmacy ID",pharmacyId],["Pharmacy name","WellCare Pharmacy"],["Owner name","Anil Mehta"],["License number","DL-NOI-2026-84517"],["GST number","09ABCDE1234F1Z5"],["Contact","+91 98765 43021"],["Address","Sector 18, Noida, Uttar Pradesh"],["Service areas","Noida sectors 15–62"],["Delivery radius","8 km"],["Opening time","08:00 AM"],["Closing time","10:00 PM"],["Minimum order","₹199"]]; return <Page title="Pharmacy Profile" eyebrow="Registered pharmacy" description="Maintain pharmacy identity, license, service area and timings." actions={<button className="rounded-xl bg-emerald-600 px-5 py-2.5 text-sm font-bold text-white">Save profile</button>}><Card className="p-6"><div className="grid gap-5 md:grid-cols-2">{fields.map(([label,value])=><label key={label} className="text-xs font-bold uppercase tracking-wider text-slate-500">{label}<input {...(label==="Pharmacy ID"?{value,readOnly:true}:{defaultValue:value})} className={`mt-2 h-11 w-full rounded-xl border px-4 text-sm font-medium normal-case tracking-normal outline-none ${label==="Pharmacy ID"?"border-emerald-200 bg-emerald-50 font-extrabold text-emerald-700":"border-slate-200 text-slate-800 focus:border-emerald-500"}`}/></label>)}</div><div className="mt-6 grid gap-4 sm:grid-cols-2"><label className="rounded-xl border p-4 text-sm font-bold"><input type="checkbox" defaultChecked className="mr-3"/>Home delivery available</label><label className="rounded-xl border p-4 text-sm font-bold"><input type="checkbox" defaultChecked className="mr-3"/>Store pickup available</label></div><div className="mt-6 rounded-2xl border border-dashed p-8 text-center"><Upload className="mx-auto size-7 text-emerald-600"/><p className="mt-2 font-bold">Upload logo and pharmacy documents</p><p className="text-xs text-slate-500">Drug license, GST certificate and owner identity</p></div></Card></Page>; }

export function PrescriptionDetails({ id }: { id: string }) { const request=requests.find(item=>item.id===id)||requests[0]; return <Page title={`Prescription Request ${request.id}`} eyebrow="Authorized prescription view" description="This access is limited to the prescription shared by the patient for medicine ordering." actions={<ActionLink href="/pharmacy/quotations/create" label="Create quotation"/>}><div className="grid gap-6 xl:grid-cols-[.7fr_1.3fr]"><div className="space-y-5"><Card className="p-5"><h2 className="font-black">Patient information</h2><dl className="mt-4 space-y-3 text-sm"><Info label="Name" value={request.patient}/><Info label="Location" value={request.location}/><Info label="Delivery" value={request.delivery}/></dl></Card><Card className="p-5"><h2 className="font-black">Prescriber</h2><dl className="mt-4 space-y-3 text-sm"><Info label="Doctor" value={request.doctor}/><Info label="Facility" value={request.facility}/><Info label="Prescription" value={request.prescription}/></dl></Card><Card className="grid min-h-64 place-items-center bg-slate-950 p-6 text-center text-white"><div><FileText className="mx-auto size-10 text-emerald-400"/><p className="mt-3 font-bold">Prescription preview</p><p className="text-xs text-slate-400">Secure patient-shared document</p></div></Card></div><Card><div className="border-b p-5"><h2 className="font-black">Prescribed medicines</h2><p className="mt-1 text-xs text-slate-500">Availability can be updated; prescription details cannot be edited.</p></div><div className="divide-y">{medicines.map(item=><div key={item.name} className="p-5"><div className="flex flex-wrap justify-between gap-3"><div><h3 className="font-black text-slate-900">{item.name}</h3><p className="mt-1 text-sm text-slate-500">{item.dosage} · {item.frequency} · {item.duration}</p></div><Badge>{item.availability}</Badge></div><div className="mt-4 grid gap-3 rounded-xl bg-slate-50 p-4 text-xs sm:grid-cols-3"><Info label="Required quantity" value={String(item.quantity)}/><Info label="Instruction" value={item.instruction}/><Info label="Doctor note" value="Use exactly as prescribed"/></div></div>)}</div></Card></div></Page>; }

export function QuotationCreate() { const router=useRouter(); const [delivery]=useState(49); const [action,setAction]=useState<"draft"|"send"|"reject"|null>(null); const subtotal=medicines.reduce((sum,item)=>sum+item.quantity*12,0); const gst=Math.round(subtotal*.05); const finalPayable=subtotal-54+gst+delivery; const finishAction=(type:"draft"|"send"|"reject")=>{ if(type==="reject"&&!window.confirm("Reject this prescription request?")) return; setAction(type); const now=new Date(); const status=type==="draft"?"Draft":type==="send"?"Quotation Sent":"Rejected"; const quotationId=`QUO-${String(now.getFullYear()).slice(-2)}${String(now.getMonth()+1).padStart(2,"0")}${String(now.getDate()).padStart(2,"0")}-${String(now.getTime()).slice(-4)}`; try { const saved=JSON.parse(localStorage.getItem("pharmacyQuotationActions")||"[]"); localStorage.setItem("pharmacyQuotationActions",JSON.stringify([{id:quotationId,requestId:requests[0]?.id,patient:requests[0]?.patient,prescription:requests[0]?.prescription,status,amount:finalPayable,formattedAmount:`₹${finalPayable.toLocaleString("en-IN")}`,sent:now.toLocaleDateString("en-IN",{day:"2-digit",month:"short",year:"numeric"}),valid:"6 hours",response:type==="draft"?"Not sent":"Awaiting response",updatedAt:now.toISOString()},...saved].slice(0,50))); } catch {} toast[type==="reject"?"error":"success"](type==="draft"?"Quotation saved as draft.":type==="send"?"Quotation sent to the patient successfully.":"Prescription request rejected."); router.push(type==="reject"?"/pharmacy/prescription-requests":"/pharmacy/quotations"); }; return <Page title="Create Quotation" eyebrow="Prescription pricing" description="Enter availability and pricing. Any alternative medicine will require patient approval." actions={<ActionLink href="/pharmacy/quotations" label="Back to quotations"/>}><div className="grid gap-6 xl:grid-cols-[1.4fr_.6fr]"><Card><div className="border-b p-5"><h2 className="font-black">Medicine pricing</h2></div><div className="space-y-4 p-5">{medicines.map((item,index)=><div key={item.name} className="grid gap-3 rounded-2xl border bg-slate-50/50 p-4 md:grid-cols-2 xl:grid-cols-4"><Field label="Medicine" value={item.name}/><Field label="Brand" value={index===0?"Telma":"Generic equivalent"}/><Field label="Quantity" value={String(item.quantity)}/><Field label="MRP" value="₹12.00"/><Field label="Discount" value="5%"/><Field label="GST" value="5%"/><Field label="Final price" value="₹11.97"/><label className="text-xs font-bold text-slate-500">Availability<select className="mt-2 h-10 w-full rounded-lg border bg-white px-3 text-sm"><option>{item.availability}</option><option>Available</option><option>Partially Available</option><option>Not Available</option><option>Alternative Available</option></select></label></div>)}</div></Card><div className="space-y-5"><Card className="p-5"><h2 className="font-black">Quotation summary</h2><div className="mt-5 space-y-3 text-sm"><Info label="Medicine subtotal" value={`₹${subtotal}`}/><Info label="Total discount" value="− ₹54"/><Info label="GST" value={`₹${gst}`}/><Info label="Delivery charge" value={`₹${delivery}`}/><div className="border-t pt-4"><Info label="Final payable" value={`₹${finalPayable}`}/></div></div></Card><Card className="space-y-4 p-5"><Field label="Estimated delivery" value="45–60 minutes"/><Field label="Quotation expiry" value="6 hours"/><Field label="Pharmacy notes" value="All available medicines are from verified batches."/><button type="button" disabled={action!==null} onClick={()=>finishAction("draft")} className="w-full rounded-xl border py-3 text-sm font-bold transition hover:bg-slate-50 disabled:opacity-50">{action==="draft"?"Saving...":"Save Draft"}</button><button type="button" disabled={action!==null} onClick={()=>finishAction("send")} className="w-full rounded-xl bg-emerald-600 py-3 text-sm font-bold text-white transition hover:bg-emerald-700 disabled:opacity-50"><Send className="mr-2 inline size-4"/>{action==="send"?"Sending...":"Send Quotation"}</button><button type="button" disabled={action!==null} onClick={()=>finishAction("reject")} className="w-full rounded-xl bg-rose-50 py-3 text-sm font-bold text-rose-700 transition hover:bg-rose-100 disabled:opacity-50">{action==="reject"?"Rejecting...":"Reject Request"}</button></Card></div></div></Page>; }

const Info=({label,value}:{label:string;value:string})=><div className="flex justify-between gap-3"><dt className="text-slate-500">{label}</dt><dd className="text-right font-bold text-slate-800">{value}</dd></div>;
const Field=({label,value}:{label:string;value:string})=><label className="text-xs font-bold text-slate-500">{label}<input defaultValue={value} className="mt-2 h-10 w-full rounded-lg border bg-white px-3 text-sm font-medium text-slate-800 outline-none focus:border-emerald-500"/></label>;
