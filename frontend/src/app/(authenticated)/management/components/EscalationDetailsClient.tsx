"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import { toast } from "sonner";

export default function EscalationDetailsClient({ role, id }: { role: "admin" | "accounts"; id: string }) {
  const [data, setData] = useState<any>(null);
  const [note, setNote] = useState("");
  const [saving, setSaving] = useState(false);
  const token = () => localStorage.getItem("token") || "";
  const load = () => fetch(`/api/support-tickets/${id}`, { headers: { Authorization: `Bearer ${token()}` } }).then(async response => { if (!response.ok) throw new Error("Escalation was not found."); return response.json(); }).then(setData).catch(error => toast.error(error.message));
  useEffect(() => { load(); }, [id]);
  const updateStatus = async (status: string) => { setSaving(true); try { const response = await fetch(`/api/support-tickets/${id}/status`, { method: "PUT", headers: { "Content-Type": "application/json", Authorization: `Bearer ${token()}` }, body: JSON.stringify({ status }) }); if (!response.ok) throw new Error("Status update failed."); await load(); toast.success(`Ticket marked ${status}.`); } catch (error) { toast.error(error instanceof Error ? error.message : "Update failed."); } finally { setSaving(false); } };
  const saveNote = async () => { if (!note.trim()) return; setSaving(true); try { const response = await fetch(`/api/support-tickets/${id}/details`, { method: "PUT", headers: { "Content-Type": "application/json", Authorization: `Bearer ${token()}` }, body: JSON.stringify({ assignedTo: data?.details?.assignedTo || `${role === 'admin' ? 'Admin' : 'Accounts'} Team`, internalNotes: note }) }); if (!response.ok) throw new Error("Note could not be saved."); setNote(""); await load(); toast.success("Internal note saved."); } catch (error) { toast.error(error instanceof Error ? error.message : "Note failed."); } finally { setSaving(false); } };
  if (!data) return <div className="p-10 text-center text-slate-500 animate-pulse">Loading escalation from database…</div>;
  const { ticket, details, replies } = data;
  return <div className="mx-auto min-h-screen max-w-5xl p-6 md:p-8"><Link href={`/management/${role}/assigned-escalations`} className="text-sm font-bold text-cyan-700">← Assigned escalations</Link><div className="mt-6 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="flex items-center gap-3"><h1 className="text-2xl font-black text-slate-900">{ticket.ticketId}</h1><span className="rounded-full bg-rose-50 px-3 py-1 text-xs font-bold text-rose-600">{ticket.priority}</span></div><p className="mt-2 text-sm text-slate-500">{ticket.userName} · {ticket.userRole} · {ticket.category}</p></div><button disabled={saving || ['resolved','closed'].includes(String(ticket.status).toLowerCase())} onClick={() => updateStatus('Resolved')} className="rounded-xl bg-emerald-600 px-4 py-2.5 text-sm font-bold text-white disabled:opacity-40">Mark resolved</button></div></div><div className="mt-6 grid gap-6 lg:grid-cols-[1fr_20rem]"><main className="space-y-6"><section className="rounded-2xl border border-slate-200 bg-white p-6"><h2 className="text-lg font-black text-slate-900">{ticket.subject}</h2><p className="mt-3 whitespace-pre-wrap text-sm leading-6 text-slate-600">{ticket.description}</p></section><section className="rounded-2xl border border-slate-200 bg-white p-6"><h2 className="font-black text-slate-900">Ticket conversation</h2><div className="mt-4 space-y-3">{(replies || []).map((reply: any) => <div key={reply.id} className="rounded-xl bg-slate-50 p-4"><div className="flex justify-between text-xs"><b>{reply.senderName} · {reply.senderRole}</b><time className="text-slate-400">{new Date(reply.createdAt).toLocaleString('en-IN')}</time></div><p className="mt-2 text-sm text-slate-600">{reply.message}</p></div>)}{!replies?.length && <p className="text-sm text-slate-500">No replies recorded.</p>}</div></section><section className="rounded-2xl border border-slate-200 bg-white p-6"><h2 className="font-black text-slate-900">Internal note</h2>{details?.internalNotes && <p className="mt-3 rounded-xl bg-amber-50 p-4 text-sm text-amber-900">{details.internalNotes}</p>}<textarea value={note} onChange={event => setNote(event.target.value)} rows={4} className="mt-4 w-full rounded-xl border border-slate-200 p-4" placeholder="Persist an internal investigation note"/><button disabled={saving || !note.trim()} onClick={saveNote} className="mt-3 rounded-xl bg-slate-900 px-5 py-3 text-sm font-bold text-white disabled:opacity-40">Save note</button></section></main><aside className="h-fit rounded-2xl border border-slate-200 bg-white p-5"><h2 className="font-black text-slate-900">Escalation details</h2><dl className="mt-5 space-y-4 text-sm"><div><dt className="text-slate-500">Status</dt><dd className="font-bold text-slate-900">{ticket.status}</dd></div><div><dt className="text-slate-500">Assigned team</dt><dd className="font-bold text-slate-900">{details?.assignedTo || 'Unassigned'}</dd></div><div><dt className="text-slate-500">Created</dt><dd className="font-bold text-slate-900">{new Date(ticket.createdAt).toLocaleString('en-IN')}</dd></div><div><dt className="text-slate-500">Last updated</dt><dd className="font-bold text-slate-900">{new Date(ticket.updatedAt).toLocaleString('en-IN')}</dd></div></dl></aside></div></div>;
}
