# Legacy frontend utilities

This folder contains one-time maintenance artifacts that are not imported by
or executed as part of the Next.js application.

- `fix_modals.js` is a historical mechanical source rewrite utility.
- `sql/` contains historical schema drafts; the active MySQL schema migrations
  are maintained exclusively in `backend/migrations/`.
