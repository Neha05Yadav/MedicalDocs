# MediDoc Frontend

Next.js App Router frontend for patient, clinic, hospital, laboratory, and management dashboards.

Run commands from the repository root with `npm run dev:frontend` and `npm run build:frontend`, or use the scripts in this directory's `package.json` directly.
